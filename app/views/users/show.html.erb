<% provide(:title, @user.name) %>
<div class="row">
  <aside class="col-md-4">
    <section class="user_info">
      <h2>
        <%= gravatar_for @user %>
        <%= @user.name %>
      </h2>
    </section>
  </aside>
  <div class="col-md-8">
    <% if @user.tournaments.any? %>
      <h3>作成した大会 (<%= @user.tournaments.count %>)</h3>
      <ol class="tournaments">
        <%= render @tournaments %>
      </ol>
      <%= will_paginate @tournaments %>
    <% end %>
    
  </div>
</div>
