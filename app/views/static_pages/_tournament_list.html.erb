<div class="row">
  <div class="col-md-12">
    <h3>大会一覧</h3>
    <% if @tournaments.any? %>
      <ol class="tournaments">
        <%= render @tournaments %>
      </ol>
      <%= will_paginate @tournaments, params: { controller: :static_pages, action: :home } %>
    <% end %>
  </div>
</div>
