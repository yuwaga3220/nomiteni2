<%= @user.name %>様

パスワード再設定のリクエストを受け付けました。以下のURLから再設定してください。

<%= edit_password_reset_url(@user.reset_token, email: @user.email) %>

このリンクの有効期限は2時間です。

パスワードの再設定に心当たりがない場合は、このメールを無視してください。
パスワードはそのまま変更されません。
