<%= link_to gravatar_for(current_user, size: 50), current_user %>
<h2><%= current_user.name %></h2>
<span><%= link_to "プロフィールを見る", current_user %></span>
<span><%= current_user.tournaments.count %> 大会</span>